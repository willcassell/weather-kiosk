# =============================================================================
# WeatherFlow Tempest Weather Kiosk - Configuration File
# =============================================================================
# Copy this file to .env and customize with your settings
# Required fields are marked with (REQUIRED)
# Optional fields can be left blank or removed if not needed
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
# PostgreSQL connection string
# For Docker: Use the service name 'postgres' as the host
# SECURITY WARNING: Generate a strong database password! Never use the example below.
# Generate with: openssl rand -base64 24
POSTGRES_PASSWORD=CHANGE_THIS_STRONG_PASSWORD
DATABASE_URL=postgresql://weather_user:${POSTGRES_PASSWORD}@postgres:5432/weather_kiosk

# -----------------------------------------------------------------------------
# APPLICATION CONFIGURATION
# -----------------------------------------------------------------------------
NODE_ENV=production
PORT=5000

# Session secret for secure session handling (REQUIRED)
# This encrypts user session data - must be a random 64-character hex string
#
# To generate on Mac/Linux, run this command in your terminal:
#   openssl rand -hex 32
#
# To generate on Windows (PowerShell), run:
#   -join ((48..57) + (97..102) | Get-Random -Count 64 | ForEach-Object {[char]$_})
#
# Or use an online generator: https://generate-secret.vercel.app/32
SESSION_SECRET=your_random_session_secret_here

# -----------------------------------------------------------------------------
# WEATHERFLOW TEMPEST CONFIGURATION (REQUIRED)
# -----------------------------------------------------------------------------
# Get your personal access token at: https://tempestwx.com/settings/tokens
# Find your station ID in your Tempest app or at: https://tempestwx.com/station/
WEATHERFLOW_API_TOKEN=your_weatherflow_token_here
WEATHERFLOW_STATION_ID=your_station_id_here

# Optional: Custom display name for your station
# If not set, will use the station name from WeatherFlow API
# Example: VITE_STATION_DISPLAY_NAME=Home Weather Station
VITE_STATION_DISPLAY_NAME=

# -----------------------------------------------------------------------------
# UNIT PREFERENCES (for International Users)
# -----------------------------------------------------------------------------
# Choose your preferred unit system: "imperial" or "metric"
#
# imperial (US/UK):
#   - Temperature: Fahrenheit (°F)
#   - Wind Speed: Miles per hour (mph)
#   - Precipitation: Inches (in)
#   - Pressure: Inches of mercury (inHg)
#   - Distance: Miles (mi)
#
# metric (International):
#   - Temperature: Celsius (°C)
#   - Wind Speed: Kilometers per hour (km/h)
#   - Precipitation: Millimeters (mm)
#   - Pressure: Hectopascals/Millibars (hPa/mb)
#   - Distance: Kilometers (km)
#
VITE_UNIT_SYSTEM=imperial

# -----------------------------------------------------------------------------
# TIMEZONE CONFIGURATION (for International Users)
# -----------------------------------------------------------------------------
# Set your local timezone for accurate daily high/low temperature calculations
# Use IANA timezone names (examples below):
#
# North America:
#   - America/New_York (US Eastern)
#   - America/Chicago (US Central)
#   - America/Denver (US Mountain)
#   - America/Los_Angeles (US Pacific)
#   - America/Toronto (Canada Eastern)
#
# Europe:
#   - Europe/London (UK)
#   - Europe/Paris (France/Central Europe)
#   - Europe/Berlin (Germany)
#   - Europe/Rome (Italy)
#
# Asia/Pacific:
#   - Asia/Tokyo (Japan)
#   - Asia/Shanghai (China)
#   - Asia/Singapore (Singapore)
#   - Australia/Sydney (Australia Eastern)
#
# Full list: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
# If not set, defaults to America/New_York (Eastern Time)
TIMEZONE=America/New_York

# -----------------------------------------------------------------------------
# WEATHER RADAR CONFIGURATION (REQUIRED)
# -----------------------------------------------------------------------------
# Set the center coordinates and zoom level for your radar display
# Find your coordinates: https://www.latlong.net/
# Zoom levels: 5-10 (higher = more zoomed in, 7.25 is a good default)
VITE_RADAR_CENTER_LAT=37.000
VITE_RADAR_CENTER_LON=-78.415
VITE_RADAR_ZOOM_LEVEL=7.25

# -----------------------------------------------------------------------------
# INDOOR CLIMATE / THERMOSTAT CONFIGURATION (OPTIONAL)
# -----------------------------------------------------------------------------
# Enable indoor climate monitoring with Ecobee thermostats via Beestat API
# Leave BEESTAT_API_KEY blank to disable the thermostat card entirely

# Get your Beestat API key at: https://beestat.io/account
# Note: You must first link your Ecobee account to Beestat
BEESTAT_API_KEY=

# Comma-separated list of thermostat names to display (must match exactly)
# Example: TARGET_THERMOSTAT_NAMES=Downstairs,Upstairs,Main Floor
TARGET_THERMOSTAT_NAMES=

# -----------------------------------------------------------------------------
# CLOUDFLARE TUNNEL CONFIGURATION (OPTIONAL)
# -----------------------------------------------------------------------------
# Securely expose your kiosk to the internet without opening firewall ports
# Create a tunnel at: https://one.dash.cloudflare.com/ → Networks → Tunnels
# Leave blank if you don't need internet access
CLOUDFLARE_TUNNEL_TOKEN=

# -----------------------------------------------------------------------------
# GRAFANA CONFIGURATION (OPTIONAL)
# -----------------------------------------------------------------------------
# Grafana admin credentials for the web interface
# Access Grafana at: http://localhost:3000
# Default: admin/admin (change these for production!)
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin
